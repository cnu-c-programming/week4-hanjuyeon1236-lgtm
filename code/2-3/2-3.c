#include <stdio.h>
#include

int main() {
  char a[64] = "Hello";
  char b[64] = "World";
  char c[128];

  printf("length of a: %d\n", len);

  printf("a: %s, c: %s\n", a, c);

  printf("c: %s\n", c);

  printf("strcmp(%s, %s) = %d\n", a, a, );
  printf("strcmp(%s, %s) = %d/n", a, b, );

  return 0;
}
