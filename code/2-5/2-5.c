#include <stdio.h>
#include

int main() {
  double x = 9.0;
  double y = 2.5;

  /* sqrt: 제곱근 */
  printf(%.2f\n",  (x));
  /* pow: 거듭제곱 */
  printf("%.2f\n",  (x, y));
  /* ceil: 올림 */
  printf("%.1f\n",  (y));
  /* floor: 내림 */
  printf("%.1f\n",  (y));

  return 0;
}
